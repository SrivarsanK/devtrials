{
	"name": "devtrials",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "pnpm --filter web dev",
		"dev:dashboard": "pnpm --filter insurer-dashboard dev",
		"build": "pnpm --filter web build",
		"start": "pnpm --filter web start",
		"kill-ports": "npx kill-port 3000 3001 3002 3005",
		"clean": "Get-ChildItem -Path . -Include .next,node_modules,dist -Recurse | Remove-Item -Force -Recurse"
	},
	"dependencies": {
		"next": "16.2.2",
		"react": "19.2.4",
		"react-dom": "19.2.4"
	},
	"devDependencies": {
		"next": "16.2.2"
	}
}
