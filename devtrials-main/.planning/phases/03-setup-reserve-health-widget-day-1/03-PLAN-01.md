---
phase: 3
plan: 1
title: "Next.js Project Scaffold & Shared Foundation"
wave: 1
depends_on: []
files_modified:
  - apps/insurer-dashboard/package.json
  - apps/insurer-dashboard/src/types/index.ts
  - apps/insurer-dashboard/src/constants/thresholds.ts
  - apps/insurer-dashboard/src/constants/zones.ts
  - apps/insurer-dashboard/src/constants/tiers.ts
  - apps/insurer-dashboard/.env.local
  - apps/insurer-dashboard/.env.example
  - apps/insurer-dashboard/tailwind.config.ts
  - apps/insurer-dashboard/src/app/globals.css
  - apps/insurer-dashboard/src/lib/utils.ts
autonomous: true
requirements_addressed: []
---

# Plan 01: Next.js Project Scaffold & Shared Foundation

## Objective

Create the Next.js insurer-dashboard application inside the monorepo's `apps/` directory and establish all shared TypeScript types, constants, and environment configuration that every subsequent plan depends on.

## Tasks

<task id="01.1">
<title>Create Next.js Application Scaffold</title>
<read_first>
- apps/ (directory listing — confirm no existing insurer-dashboard)
- package.json (root monorepo config)
- pnpm-workspace.yaml (workspace configuration)
</read_first>
<action>
1. Run: `npx -y create-next-app@latest apps/insurer-dashboard --typescript --eslint --tailwind --src-dir --app --no-import-alias --use-npm`
2. Verify the scaffold created successfully with `src/app/page.tsx` existing
3. cd into apps/insurer-dashboard and install dependencies:
   ```
   npm install recharts d3 leaflet react-leaflet socket.io-client lucide-react zustand
   npm install @types/leaflet @types/d3 --save-dev
   ```
4. Install shadcn/ui:
   ```
   npx -y shadcn@latest init --defaults
   ```
5. Add useful shadcn components:
   ```
   npx -y shadcn@latest add card badge button switch tooltip separator
   ```
</action>
<acceptance_criteria>
- `apps/insurer-dashboard/package.json` exists and contains `"next"` in dependencies
- `apps/insurer-dashboard/package.json` contains `"zustand"` in dependencies
- `apps/insurer-dashboard/package.json` contains `"recharts"` in dependencies
- `apps/insurer-dashboard/package.json` contains `"lucide-react"` in dependencies
- `apps/insurer-dashboard/package.json` contains `"socket.io-client"` in dependencies
- `apps/insurer-dashboard/package.json` contains `"d3"` in devDependencies or dependencies
- `apps/insurer-dashboard/src/app/page.tsx` exists
- `apps/insurer-dashboard/src/app/layout.tsx` exists
- `npx next build` completes without errors from the apps/insurer-dashboard directory (or `npm run dev` starts successfully)
</acceptance_criteria>
</task>

<task id="01.2">
<title>Create Shared TypeScript Types</title>
<read_first>
- .planning/phases/03-setup-reserve-health-widget-day-1/03-CONTEXT.md (all type definitions and mock data shapes)
</read_first>
<action>
Create file `apps/insurer-dashboard/src/types/index.ts` with these exact type definitions:

```typescript
// Zone status enum
export type ZoneStatus = 'GREEN' | 'AMBER' | 'RED';

// Trigger status enum
export type TriggerStatus = 'NORMAL' | 'WATCH' | 'TRIGGERED';

// City type
export type City = 'Chennai' | 'Mumbai' | 'Delhi';

// Tier names
export type TierName = 'Basic Shield' | 'Standard Shield' | 'Full Shield';

// Zone reserve data
export interface ZoneReserve {
  id: string;
  name: string;
  city: City;
  status: ZoneStatus;
  coverageRatioPercent: number;
  reserveLakhs: number;
  liabilityLakhs: number;
  policies: number;
  predictedClaims7dLakhs: number;
  issuancesPaused: boolean;
}

// City-level reserve summary
export interface CityReserveSummary {
  city: City;
  status: ZoneStatus;
  activePolicies: number;
  reserveLakhs: number;
  liabilityLakhs: number;
  zones: ZoneReserve[];
}

// Global reserve health (top-level data shape)
export interface GlobalReserveHealth {
  reserveBalanceCrores: number;
  activeLiabilityCrores: number;
  coverageRatioPercent: number;
  lossRatioPercent: number;
  weeklyPremiumInflowLakhs: number;
  weeklyClaimsOutflowLakhs: number;
  globalStatus: ZoneStatus;
  globalMessage: string;
  cities: CityReserveSummary[];
}

// Trigger event
export interface TriggerEvent {
  id: string;
  name: string;
  thresholdLabel: string;
  currentValue: string;
  progressPercent: number;
  status: TriggerStatus;
  cities: City[];
  affectedWorkers?: number;
  estimatedPayoutLakhs?: number;
}

// Claim
export interface Claim {
  id: string;
  workerId: string;
  zoneId: string;
  city: City;
  triggerType: string;
  amountInr: number;
  status: 'PENDING' | 'APPROVED' | 'REJECTED' | 'FLAGGED';
  timestamp: string;
}

// Fraud alert
export interface FraudAlert {
  id: string;
  type: 'GPS_SPOOF' | 'ANOMALY' | 'DUPLICATE' | 'CLUSTER';
  description: string;
  city: City;
  zone: string;
  fraudScore: number;
  claimAmountInr: number;
  status: 'PENDING_REVIEW' | 'APPROVED' | 'REJECTED' | 'INVESTIGATING';
  autoAction: string;
}

// Zone forecast
export interface ZoneForecast {
  zone: string;
  city: City;
  predictedPayoutLakhs: number;
  riskStatus: ZoneStatus;
  circuitBreaker: 'ACTIVE' | 'PAUSED';
  dominantTrigger: string;
}

// Weekly forecast
export interface WeeklyForecast {
  total72hrPredictedLiabilityLakhs: number;
  zones: ZoneForecast[];
}

// Policy summary
export interface Policy {
  id: string;
  workerId: string;
  tier: TierName;
  city: City;
  zone: string;
  weeklyPremiumInr: number;
  status: 'ACTIVE' | 'PAUSED' | 'EXPIRED';
  startDate: string;
}

// Reinsurance alert
export interface ReinsuranceAlert {
  partner: string;
  triggerLossRatioPercent: number;
  currentStatus: 'Normal' | 'Pre-alerted' | 'Claim Initiated';
  coverageLimitCrores: number;
  alertSentTimestamp?: string;
}

// Loss ratio data point (for chart)
export interface LossRatioDataPoint {
  date: string;
  lossRatioPercent: number;
  premiumsCollectedLakhs: number;
  claimsPaidLakhs: number;
}

// Analytics summary
export interface AnalyticsSummary {
  activePolicies: number;
  totalPremiumsCollectedCrores: number;
  totalClaimsPaidCrores: number;
  overallLossRatioPercent: number;
}
```
</action>
<acceptance_criteria>
- `apps/insurer-dashboard/src/types/index.ts` exists
- File contains `export type ZoneStatus`
- File contains `export interface GlobalReserveHealth`
- File contains `export interface ZoneReserve`
- File contains `export interface CityReserveSummary`
- File contains `export interface TriggerEvent`
- File contains `export interface FraudAlert`
- File contains `export interface ZoneForecast`
- File contains `export interface LossRatioDataPoint`
- File contains `export interface ReinsuranceAlert`
</acceptance_criteria>
</task>

<task id="01.3">
<title>Create Shared Constants — Thresholds</title>
<read_first>
- .planning/phases/03-setup-reserve-health-widget-day-1/03-CONTEXT.md (threshold values)
</read_first>
<action>
Create file `apps/insurer-dashboard/src/constants/thresholds.ts`:

```typescript
// Rainfall trigger thresholds
export const RAINFALL_THRESHOLD_3HR = 35; // mm
export const RAINFALL_THRESHOLD_24HR = 64.5; // mm

// Heat trigger thresholds
export const HEAT_TEMP_THRESHOLD = 42; // °C
export const HEAT_INDEX_THRESHOLD = 54; // °C

// AQI threshold
export const AQI_THRESHOLD = 300;

// Coverage ratio thresholds (as decimal ratios)
export const COVERAGE_RATIO_GREEN = 1.2; // >= 120% is GREEN
export const COVERAGE_RATIO_AMBER = 0.9; // >= 90% is AMBER, below is RED

// Loss ratio thresholds (as percentages)
export const LOSS_RATIO_GREEN = 65; // <= 65% is GREEN
export const LOSS_RATIO_AMBER = 80; // <= 80% is AMBER, above is RED

// Fraud score thresholds
export const FRAUD_SCORE_AUTO_APPROVE = 0.3;
export const FRAUD_SCORE_MANUAL_REVIEW = 0.6;
export const FRAUD_SCORE_AUTO_DENY = 0.8;

// Reinsurance trigger
export const REINSURANCE_LOSS_RATIO_TRIGGER = 90; // percent
export const REINSURANCE_COVERAGE_LIMIT_CRORES = 5;

// Helper functions
export function getCoverageRatioStatus(ratio: number): 'GREEN' | 'AMBER' | 'RED' {
  if (ratio >= COVERAGE_RATIO_GREEN) return 'GREEN';
  if (ratio >= COVERAGE_RATIO_AMBER) return 'AMBER';
  return 'RED';
}

export function getLossRatioStatus(ratio: number): 'GREEN' | 'AMBER' | 'RED' {
  if (ratio <= LOSS_RATIO_GREEN) return 'GREEN';
  if (ratio <= LOSS_RATIO_AMBER) return 'AMBER';
  return 'RED';
}
```
</action>
<acceptance_criteria>
- `apps/insurer-dashboard/src/constants/thresholds.ts` exists
- File contains `RAINFALL_THRESHOLD_3HR = 35`
- File contains `COVERAGE_RATIO_GREEN = 1.2`
- File contains `LOSS_RATIO_GREEN = 65`
- File contains `FRAUD_SCORE_AUTO_DENY = 0.8`
- File contains `export function getCoverageRatioStatus`
- File contains `export function getLossRatioStatus`
</acceptance_criteria>
</task>

<task id="01.4">
<title>Create Shared Constants — Zones</title>
<read_first>
- .planning/phases/03-setup-reserve-health-widget-day-1/03-CONTEXT.md (zone definitions)
</read_first>
<action>
Create file `apps/insurer-dashboard/src/constants/zones.ts`:

```typescript
import { City } from '@/types';

export interface ZoneDefinition {
  id: string;
  name: string;
  city: City;
  riskScore: number;
  multiplier: number;
  coordinates: { lat: number; lng: number };
}

export const ZONES: ZoneDefinition[] = [
  // Chennai
  { id: 'CHN_ANNA_NAGAR', name: 'Anna Nagar', city: 'Chennai', riskScore: 7, multiplier: 1.20, coordinates: { lat: 13.0850, lng: 80.2101 } },
  { id: 'CHN_T_NAGAR', name: 'T Nagar', city: 'Chennai', riskScore: 9, multiplier: 1.40, coordinates: { lat: 13.0418, lng: 80.2341 } },
  { id: 'CHN_TAMBARAM', name: 'Tambaram', city: 'Chennai', riskScore: 4, multiplier: 1.00, coordinates: { lat: 12.9249, lng: 80.1000 } },
  // Mumbai
  { id: 'MUM_DHARAVI', name: 'Dharavi', city: 'Mumbai', riskScore: 7, multiplier: 1.20, coordinates: { lat: 19.0432, lng: 72.8527 } },
  { id: 'MUM_ANDHERI', name: 'Andheri', city: 'Mumbai', riskScore: 5, multiplier: 1.00, coordinates: { lat: 19.1136, lng: 72.8697 } },
  // Delhi
  { id: 'DEL_LAXMI_NAGAR', name: 'Laxmi Nagar', city: 'Delhi', riskScore: 6, multiplier: 1.00, coordinates: { lat: 28.6304, lng: 77.2772 } },
  { id: 'DEL_KAROL_BAGH', name: 'Karol Bagh', city: 'Delhi', riskScore: 5, multiplier: 1.00, coordinates: { lat: 28.6514, lng: 77.1907 } },
];

export const CITIES: City[] = ['Chennai', 'Mumbai', 'Delhi'];

export function getZonesByCity(city: City): ZoneDefinition[] {
  return ZONES.filter((z) => z.city === city);
}

export function getZoneById(id: string): ZoneDefinition | undefined {
  return ZONES.find((z) => z.id === id);
}
```
</action>
<acceptance_criteria>
- `apps/insurer-dashboard/src/constants/zones.ts` exists
- File contains `CHN_ANNA_NAGAR`
- File contains `MUM_DHARAVI`
- File contains `DEL_LAXMI_NAGAR`
- File contains `export function getZonesByCity`
- File contains `export function getZoneById`
</acceptance_criteria>
</task>

<task id="01.5">
<title>Create Shared Constants — Tiers</title>
<read_first>
- .planning/phases/03-setup-reserve-health-widget-day-1/03-CONTEXT.md (tier definitions)
</read_first>
<action>
Create file `apps/insurer-dashboard/src/constants/tiers.ts`:

```typescript
import { TierName } from '@/types';

export interface TierDefinition {
  name: TierName;
  baseWeeklyPremiumInr: number;
  coverageRatePercent: number;
  monthlyEventCap: number;
  maxPayoutPerEventInr: number;
  peakHourWeighting: boolean;
  triggersCovered: number;
  payoutSpeedHours: number;
  carryForward: boolean;
}

export const TIERS: TierDefinition[] = [
  {
    name: 'Basic Shield',
    baseWeeklyPremiumInr: 79,
    coverageRatePercent: 50,
    monthlyEventCap: 2,
    maxPayoutPerEventInr: 500,
    peakHourWeighting: false,
    triggersCovered: 4,
    payoutSpeedHours: 24,
    carryForward: false,
  },
  {
    name: 'Standard Shield',
    baseWeeklyPremiumInr: 119,
    coverageRatePercent: 70,
    monthlyEventCap: 3,
    maxPayoutPerEventInr: 800,
    peakHourWeighting: true,
    triggersCovered: 6,
    payoutSpeedHours: 12,
    carryForward: false,
  },
  {
    name: 'Full Shield',
    baseWeeklyPremiumInr: 159,
    coverageRatePercent: 80,
    monthlyEventCap: 4,
    maxPayoutPerEventInr: 1200,
    peakHourWeighting: true,
    triggersCovered: 6,
    payoutSpeedHours: 2,
    carryForward: true,
  },
];

export const PEAK_HOUR_MULTIPLIERS = [
  { window: '11:30 AM – 2:30 PM', multiplier: 1.5, reason: 'Lunch rush' },
  { window: '2:30 PM – 6:30 PM', multiplier: 0.7, reason: 'Afternoon lull' },
  { window: '6:30 PM – 10:30 PM', multiplier: 2.0, reason: 'Dinner rush' },
  { window: '10:30 PM – 11:30 AM', multiplier: 0.5, reason: 'Off-peak' },
];
```
</action>
<acceptance_criteria>
- `apps/insurer-dashboard/src/constants/tiers.ts` exists
- File contains `Basic Shield`
- File contains `Standard Shield`
- File contains `Full Shield`
- File contains `PEAK_HOUR_MULTIPLIERS`
- File contains `baseWeeklyPremiumInr: 79`
- File contains `baseWeeklyPremiumInr: 119`
- File contains `baseWeeklyPremiumInr: 159`
</acceptance_criteria>
</task>

<task id="01.6">
<title>Create Environment Variable Files</title>
<read_first>
- .planning/phases/03-setup-reserve-health-widget-day-1/03-CONTEXT.md (env vars section)
</read_first>
<action>
Create file `apps/insurer-dashboard/.env.local`:
```
NEXT_PUBLIC_USE_MOCK=true
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
```

Create file `apps/insurer-dashboard/.env.example`:
```
# ShieldGuard Insurer Dashboard Environment Variables
# Copy this file to .env.local and fill in values

NEXT_PUBLIC_USE_MOCK=true
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
```

Ensure `.env.local` is in `.gitignore`.
</action>
<acceptance_criteria>
- `apps/insurer-dashboard/.env.local` contains `NEXT_PUBLIC_USE_MOCK=true`
- `apps/insurer-dashboard/.env.example` contains `NEXT_PUBLIC_USE_MOCK`
- `apps/insurer-dashboard/.env.example` contains `NEXT_PUBLIC_API_URL`
- `.gitignore` contains `.env.local` or `*.local`
</acceptance_criteria>
</task>

<task id="01.7">
<title>Configure TailwindCSS Theme & Global Styles</title>
<read_first>
- apps/insurer-dashboard/tailwind.config.ts (default config from scaffold)
- apps/insurer-dashboard/src/app/globals.css (default styles)
</read_first>
<action>
Update `tailwind.config.ts` to extend the theme with ShieldGuard dashboard colors:

```typescript
// Extend theme.colors with:
colors: {
  shield: {
    green: '#10B981',
    amber: '#F59E0B',
    red: '#EF4444',
    blue: '#3B82F6',
    dark: '#0F172A',
    card: '#1E293B',
    surface: '#334155',
    muted: '#94A3B8',
    border: '#475569',
  }
}
```

Update `globals.css` to include:
- Dark background (slate-900 / #0F172A)
- Base typography using Inter font from Google Fonts
- Pulsing animation keyframes for RED status dots:
```css
@keyframes pulse-red {
  0%, 100% { opacity: 1; box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.7); }
  50% { opacity: 0.8; box-shadow: 0 0 0 8px rgba(239, 68, 68, 0); }
}
@keyframes pulse-amber {
  0%, 100% { opacity: 1; box-shadow: 0 0 0 0 rgba(245, 158, 11, 0.7); }
  50% { opacity: 0.8; box-shadow: 0 0 0 6px rgba(245, 158, 11, 0); }
}
.animate-pulse-red { animation: pulse-red 2s cubic-bezier(0.4, 0, 0.6, 1) infinite; }
.animate-pulse-amber { animation: pulse-amber 2s cubic-bezier(0.4, 0, 0.6, 1) infinite; }
```
</action>
<acceptance_criteria>
- `tailwind.config.ts` contains `shield` color namespace
- `tailwind.config.ts` contains `'#10B981'` (green)
- `tailwind.config.ts` contains `'#0F172A'` (dark)
- `globals.css` contains `@keyframes pulse-red`
- `globals.css` contains `.animate-pulse-red`
- `globals.css` contains `.animate-pulse-amber`
</acceptance_criteria>
</task>

## Verification

After all tasks complete:
1. `apps/insurer-dashboard/` directory exists with full Next.js scaffold
2. All type definitions compile without TypeScript errors
3. All constants import successfully from their respective files
4. `npm run dev` starts the dev server without errors

## must_haves
- Working Next.js application scaffold with all dependencies installed
- Complete TypeScript type definitions matching API response shapes
- All threshold constants defined centrally (never hardcoded in components)
- Environment variable configuration for mock/real switching
- Custom TailwindCSS theme with ShieldGuard color palette
- Pulsing animation CSS for RED/AMBER status indicators
